services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: oculus
      POSTGRES_PASSWORD: oculus
      POSTGRES_DB: oculus
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
